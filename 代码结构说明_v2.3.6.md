# 记工App 代码结构说明 v2.3.6

## 项目概述

- **语言**：Kotlin
- **UI框架**：Jetpack Compose (Material 3)
- **本地数据库**：Room (SQLite)
- **最低SDK**：API 26 (Android 8.0)
- **目标SDK**：API 34 (Android 14)
- **版本号**：v2.3.6 (versionCode: 2360)

## 目录结构

```
app/src/main/java/com/worklogger/app/
├── MainActivity.kt              # 主Activity，底部导航，路由管理
├── WorkLoggerApp.kt             # Application类，初始化数据库和仓库
│
├── model/
│   └── Models.kt                # 所有数据模型
│       ├── WorkRecord           # 记工记录实体
│       ├── QuickPhrase          # 快捷短语实体
│       ├── UserSettings         # 用户设置数据类
│       ├── AdvanceSalaryRecord  # 预支工资记录实体
│       ├── AdvancePurchaseRecord # 垫资购买记录实体 (新增)
│       ├── StatsData            # 统计数据类
│       ├── MonthlyStats         # 月度统计数据类
│       └── MonthlySalarySettlement # 月度工资结算单
│
├── data/
│   ├── local/
│   │   ├── AppDatabase.kt       # Room数据库 (v4, 含迁移)
│   │   ├── Dao.kt               # DAO接口集合
│   │   │   ├── WorkRecordDao    # 记工记录DAO
│   │   │   ├── QuickPhraseDao   # 快捷短语DAO
│   │   │   └── AdvanceSalaryDao # 预支工资DAO
│   │   ├── AdvancePurchaseDao.kt # 垫资购买DAO (新增)
│   │   └── SettingsDataStore.kt # DataStore设置存储
│   └── repository/
│       ├── WorkRepository.kt    # 数据仓库（统一数据访问层）
│       └── SettingsRepository.kt # 设置仓库
│
├── ui/
│   ├── home/                    # 记工主页
│   │   ├── HomeScreen.kt
│   │   └── HomeViewModel.kt
│   ├── stats/                   # 统计页
│   │   ├── StatsScreen.kt
│   │   ├── StatsViewModel.kt
│   │   └── SettlementDialog.kt
│   ├── advance/                 # 预支工资页
│   │   ├── AdvanceSalaryScreen.kt
│   │   ├── AdvanceSalaryViewModel.kt
│   │   └── AddAdvanceDialog.kt
│   ├── purchase/                # 垫资购买页 (新增)
│   │   ├── PurchaseScreen.kt
│   │   ├── PurchaseViewModel.kt
│   │   └── AddPurchaseDialog.kt
│   ├── calendar/                # 日历页
│   │   ├── CalendarScreen.kt
│   │   └── CalendarViewModel.kt
│   ├── settings/                # 设置页
│   │   ├── SettingsScreen.kt
│   │   ├── SettingsViewModel.kt
│   │   ├── QuickPhraseScreen.kt
│   │   └── BatchImportDialog.kt
│   ├── trash/                   # 回收站
│   │   ├── TrashScreen.kt
│   │   └── TrashViewModel.kt
│   ├── components/              # 通用组件
│   │   ├── Components.kt        # 卡片、统计卡、空状态等
│   │   └── AddRecordDialog.kt   # 添加记工对话框
│   └── theme/                   # 主题
│       ├── Color.kt             # 颜色定义
│       ├── Theme.kt             # 主题配置
│       └── Type.kt              # 字体配置
│
├── utils/                       # 工具类
│   ├── DateUtils.kt             # 日期工具
│   ├── StatsCalculator.kt       # 统计计算
│   ├── TextRecordParser.kt      # 文本记录解析
│   ├── DataExporter.kt          # 数据导出
│   ├── DataImporter.kt          # 数据导入
│   ├── ExcelExporter.kt         # Excel导出
│   ├── UpdateChecker.kt         # 版本更新检查
│   ├── UpdateDownloader.kt      # 更新下载
│   ├── NotificationHelper.kt    # 通知管理
│   ├── ReminderReceiver.kt      # 提醒广播
│   └── BootReceiver.kt          # 开机广播
│
└── widget/
    └── QuickCheckInWidget.kt    # 桌面小组件
```

## 架构设计

### MVVM 架构
- **Model层**：`model/Models.kt` 定义所有数据实体
- **View层**：`ui/` 各模块的 `Screen.kt` 文件（Compose UI）
- **ViewModel层**：各模块的 `ViewModel.kt` 文件，管理UI状态

### 数据层
- **Room数据库**：本地SQLite存储，支持迁移
- **DataStore**：用户偏好设置存储
- **Repository模式**：统一数据访问接口

### 导航
- **Navigation Compose**：底部导航栏 + 路由
- 底部Tab：记工 → 统计 → 预支 → 购买 → 日历 → 设置

## 数据库表结构

### work_records (记工记录)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| date | TEXT | 日期 yyyy-MM-dd |
| hours | REAL | 工时 |
| isOvertime | INTEGER | 是否加班 |
| location | TEXT | 工地名称 |
| remark | TEXT | 备注 |
| mealSubsidy | INTEGER | 是否有饭补 |
| isManual | INTEGER | 是否手动折算 |
| createdAt | INTEGER | 创建时间戳 |
| updatedAt | INTEGER | 更新时间戳 |
| is_deleted | INTEGER | 软删除标记 |
| deleted_at | INTEGER | 删除时间戳 |

### advance_salary_records (预支工资)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| date | TEXT | 日期 |
| time | TEXT | 时间 |
| location | TEXT | 地点 |
| amount | REAL | 金额 |
| remark | TEXT | 备注 |
| createdAt | INTEGER | 创建时间戳 |

### advance_purchase_records (垫资购买) - 新增
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| date | TEXT | 日期 |
| itemName | TEXT | 购买名称 |
| location | TEXT | 工地名称 |
| amount | REAL | 金额 |
| quantity | INTEGER | 数量 |
| remark | TEXT | 备注 |
| createdAt | INTEGER | 创建时间戳 |
| is_deleted | INTEGER | 软删除标记 |
| deleted_at | INTEGER | 删除时间戳 |

### quick_phrases (快捷短语)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| phrase | TEXT | 短语内容 |
| useCount | INTEGER | 使用次数 |
| createdAt | INTEGER | 创建时间戳 |
